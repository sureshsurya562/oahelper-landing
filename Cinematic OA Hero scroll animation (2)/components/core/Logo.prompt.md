Official OAHelper logo (PNG, transparent): circled-A mark, mark + OAHelper, or the "Powered by NxtWave" lockup.

```jsx
<Logo />                                  // nav on ink
<Logo variant="lockup" height={30} />     // footer
<Logo variant="mark" tone="paper" height={40} />
```

Use Logo wherever the brand appears; Wordmark is a text fallback only. Keep clear space of at least the mark's radius around it. Never recolour, stretch or add effects. Set `base` if the page is not two folders below the project root.
