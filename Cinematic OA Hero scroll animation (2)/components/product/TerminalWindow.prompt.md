macOS-style terminal chrome used for the sync animation and the mock OA editor.

```jsx
<TerminalWindow><div>~/oahelper $ oa sync --companies all</div></TerminalWindow>
```

Children render in Geist Mono 15/30. Use syntax tokens (--syn-*) inside.
