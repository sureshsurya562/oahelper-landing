Three-column comparison with the OA Helper column raised as a dotted ink panel on paper.

```jsx
<CompareTable rows={[{ label: "Questions", us: "Real OA questions…", them: "Generic problem lists…" }]} />
```

For widths under ~760px stack each row instead (see UI kit).
