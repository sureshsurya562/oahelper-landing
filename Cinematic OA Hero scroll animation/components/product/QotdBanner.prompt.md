Question of the Day banner: media panel with glass countdown, difficulty, date tile, expiry pill, Solve Now and a stats row.

```jsx
<QotdBanner title="Memory Buffer Access" subtitle="Kingdom of Strings" stats={[{label:"Solvers Today",value:"39"}]} />
```

showMedia=false below 700px.
