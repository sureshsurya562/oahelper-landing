export interface WordmarkProps {
  /** Surface the mark sits on. */
  tone?: 'dark' | 'paper';
  /** Font size in px. Default 17. */
  size?: number;
}
export declare function Wordmark(props: WordmarkProps): JSX.Element;
